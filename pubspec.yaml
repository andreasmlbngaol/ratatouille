name: ratatouille
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.5+1
  get_it: ^8.2.0 # Dependency Injection
  dio: ^5.9.0 # HTTP Client
  hive: ^2.2.3 # Local Storage untuk shared preference
  hive_flutter: ^1.1.0
  firebase_core: ^4.0.0
  firebase_auth: ^6.0.1
  google_sign_in: ^6.3.0 # Jangan di upgrade Okeh
  freezed_annotation: ^3.1.0 # Data class-like
  go_router: ^16.2.0 # Main Router
  fluttertoast: ^8.2.12 # Toast
  json_annotation: ^4.9.0 # Untuk anotasi JsonKey
  #  connectivity_plus: ^7.0.0 # Cek koneksi internet 1 (bisa false positive)
  internet_connection_checker: ^3.0.1  # Cek koneksi internet
  flutter_riverpod: ^2.5.1
  skeletonizer: ^2.1.0+1
  cached_network_image: ^3.4.1
  image_picker: ^1.2.0
  google_fonts: ^6.3.2
  url_launcher: ^6.3.2
  android_intent_plus: ^6.0.0
  iconify_flutter: ^0.0.7
  dartz: ^0.10.1
  flutter_bloc: ^9.1.1
  equatable: ^2.0.7


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.10.2 # Untuk generate freezed
  freezed: ^3.2.3
  json_serializable: ^6.11.1 # Serialized data class freezed ke JSON
  flutter_launcher_icons: ^0.14.4


flutter:
  uses-material-design: true
  assets:
    - assets/images/